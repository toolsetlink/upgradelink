// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"upgradelink-admin-upgrade/server/ent/predicate"
	"upgradelink-admin-upgrade/server/ent/upgradeapkversion"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UpgradeApkVersionQuery is the builder for querying UpgradeApkVersion entities.
type UpgradeApkVersionQuery struct {
	config
	ctx        *QueryContext
	order      []upgradeapkversion.OrderOption
	inters     []Interceptor
	predicates []predicate.UpgradeApkVersion
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UpgradeApkVersionQuery builder.
func (uavq *UpgradeApkVersionQuery) Where(ps ...predicate.UpgradeApkVersion) *UpgradeApkVersionQuery {
	uavq.predicates = append(uavq.predicates, ps...)
	return uavq
}

// Limit the number of records to be returned by this query.
func (uavq *UpgradeApkVersionQuery) Limit(limit int) *UpgradeApkVersionQuery {
	uavq.ctx.Limit = &limit
	return uavq
}

// Offset to start from.
func (uavq *UpgradeApkVersionQuery) Offset(offset int) *UpgradeApkVersionQuery {
	uavq.ctx.Offset = &offset
	return uavq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (uavq *UpgradeApkVersionQuery) Unique(unique bool) *UpgradeApkVersionQuery {
	uavq.ctx.Unique = &unique
	return uavq
}

// Order specifies how the records should be ordered.
func (uavq *UpgradeApkVersionQuery) Order(o ...upgradeapkversion.OrderOption) *UpgradeApkVersionQuery {
	uavq.order = append(uavq.order, o...)
	return uavq
}

// First returns the first UpgradeApkVersion entity from the query.
// Returns a *NotFoundError when no UpgradeApkVersion was found.
func (uavq *UpgradeApkVersionQuery) First(ctx context.Context) (*UpgradeApkVersion, error) {
	nodes, err := uavq.Limit(1).All(setContextOp(ctx, uavq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{upgradeapkversion.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (uavq *UpgradeApkVersionQuery) FirstX(ctx context.Context) *UpgradeApkVersion {
	node, err := uavq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UpgradeApkVersion ID from the query.
// Returns a *NotFoundError when no UpgradeApkVersion ID was found.
func (uavq *UpgradeApkVersionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uavq.Limit(1).IDs(setContextOp(ctx, uavq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{upgradeapkversion.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (uavq *UpgradeApkVersionQuery) FirstIDX(ctx context.Context) int {
	id, err := uavq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UpgradeApkVersion entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UpgradeApkVersion entity is found.
// Returns a *NotFoundError when no UpgradeApkVersion entities are found.
func (uavq *UpgradeApkVersionQuery) Only(ctx context.Context) (*UpgradeApkVersion, error) {
	nodes, err := uavq.Limit(2).All(setContextOp(ctx, uavq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{upgradeapkversion.Label}
	default:
		return nil, &NotSingularError{upgradeapkversion.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (uavq *UpgradeApkVersionQuery) OnlyX(ctx context.Context) *UpgradeApkVersion {
	node, err := uavq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UpgradeApkVersion ID in the query.
// Returns a *NotSingularError when more than one UpgradeApkVersion ID is found.
// Returns a *NotFoundError when no entities are found.
func (uavq *UpgradeApkVersionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uavq.Limit(2).IDs(setContextOp(ctx, uavq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{upgradeapkversion.Label}
	default:
		err = &NotSingularError{upgradeapkversion.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (uavq *UpgradeApkVersionQuery) OnlyIDX(ctx context.Context) int {
	id, err := uavq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UpgradeApkVersions.
func (uavq *UpgradeApkVersionQuery) All(ctx context.Context) ([]*UpgradeApkVersion, error) {
	ctx = setContextOp(ctx, uavq.ctx, ent.OpQueryAll)
	if err := uavq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UpgradeApkVersion, *UpgradeApkVersionQuery]()
	return withInterceptors[[]*UpgradeApkVersion](ctx, uavq, qr, uavq.inters)
}

// AllX is like All, but panics if an error occurs.
func (uavq *UpgradeApkVersionQuery) AllX(ctx context.Context) []*UpgradeApkVersion {
	nodes, err := uavq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UpgradeApkVersion IDs.
func (uavq *UpgradeApkVersionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if uavq.ctx.Unique == nil && uavq.path != nil {
		uavq.Unique(true)
	}
	ctx = setContextOp(ctx, uavq.ctx, ent.OpQueryIDs)
	if err = uavq.Select(upgradeapkversion.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (uavq *UpgradeApkVersionQuery) IDsX(ctx context.Context) []int {
	ids, err := uavq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (uavq *UpgradeApkVersionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, uavq.ctx, ent.OpQueryCount)
	if err := uavq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, uavq, querierCount[*UpgradeApkVersionQuery](), uavq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (uavq *UpgradeApkVersionQuery) CountX(ctx context.Context) int {
	count, err := uavq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (uavq *UpgradeApkVersionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, uavq.ctx, ent.OpQueryExist)
	switch _, err := uavq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (uavq *UpgradeApkVersionQuery) ExistX(ctx context.Context) bool {
	exist, err := uavq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UpgradeApkVersionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (uavq *UpgradeApkVersionQuery) Clone() *UpgradeApkVersionQuery {
	if uavq == nil {
		return nil
	}
	return &UpgradeApkVersionQuery{
		config:     uavq.config,
		ctx:        uavq.ctx.Clone(),
		order:      append([]upgradeapkversion.OrderOption{}, uavq.order...),
		inters:     append([]Interceptor{}, uavq.inters...),
		predicates: append([]predicate.UpgradeApkVersion{}, uavq.predicates...),
		// clone intermediate query.
		sql:  uavq.sql.Clone(),
		path: uavq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CompanyID int `json:"company_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UpgradeApkVersion.Query().
//		GroupBy(upgradeapkversion.FieldCompanyID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (uavq *UpgradeApkVersionQuery) GroupBy(field string, fields ...string) *UpgradeApkVersionGroupBy {
	uavq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UpgradeApkVersionGroupBy{build: uavq}
	grbuild.flds = &uavq.ctx.Fields
	grbuild.label = upgradeapkversion.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CompanyID int `json:"company_id,omitempty"`
//	}
//
//	client.UpgradeApkVersion.Query().
//		Select(upgradeapkversion.FieldCompanyID).
//		Scan(ctx, &v)
func (uavq *UpgradeApkVersionQuery) Select(fields ...string) *UpgradeApkVersionSelect {
	uavq.ctx.Fields = append(uavq.ctx.Fields, fields...)
	sbuild := &UpgradeApkVersionSelect{UpgradeApkVersionQuery: uavq}
	sbuild.label = upgradeapkversion.Label
	sbuild.flds, sbuild.scan = &uavq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UpgradeApkVersionSelect configured with the given aggregations.
func (uavq *UpgradeApkVersionQuery) Aggregate(fns ...AggregateFunc) *UpgradeApkVersionSelect {
	return uavq.Select().Aggregate(fns...)
}

func (uavq *UpgradeApkVersionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range uavq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, uavq); err != nil {
				return err
			}
		}
	}
	for _, f := range uavq.ctx.Fields {
		if !upgradeapkversion.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if uavq.path != nil {
		prev, err := uavq.path(ctx)
		if err != nil {
			return err
		}
		uavq.sql = prev
	}
	return nil
}

func (uavq *UpgradeApkVersionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UpgradeApkVersion, error) {
	var (
		nodes = []*UpgradeApkVersion{}
		_spec = uavq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UpgradeApkVersion).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UpgradeApkVersion{config: uavq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, uavq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (uavq *UpgradeApkVersionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := uavq.querySpec()
	_spec.Node.Columns = uavq.ctx.Fields
	if len(uavq.ctx.Fields) > 0 {
		_spec.Unique = uavq.ctx.Unique != nil && *uavq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, uavq.driver, _spec)
}

func (uavq *UpgradeApkVersionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(upgradeapkversion.Table, upgradeapkversion.Columns, sqlgraph.NewFieldSpec(upgradeapkversion.FieldID, field.TypeInt))
	_spec.From = uavq.sql
	if unique := uavq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if uavq.path != nil {
		_spec.Unique = true
	}
	if fields := uavq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, upgradeapkversion.FieldID)
		for i := range fields {
			if fields[i] != upgradeapkversion.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := uavq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := uavq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := uavq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := uavq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (uavq *UpgradeApkVersionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(uavq.driver.Dialect())
	t1 := builder.Table(upgradeapkversion.Table)
	columns := uavq.ctx.Fields
	if len(columns) == 0 {
		columns = upgradeapkversion.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if uavq.sql != nil {
		selector = uavq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if uavq.ctx.Unique != nil && *uavq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range uavq.predicates {
		p(selector)
	}
	for _, p := range uavq.order {
		p(selector)
	}
	if offset := uavq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := uavq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UpgradeApkVersionGroupBy is the group-by builder for UpgradeApkVersion entities.
type UpgradeApkVersionGroupBy struct {
	selector
	build *UpgradeApkVersionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (uavgb *UpgradeApkVersionGroupBy) Aggregate(fns ...AggregateFunc) *UpgradeApkVersionGroupBy {
	uavgb.fns = append(uavgb.fns, fns...)
	return uavgb
}

// Scan applies the selector query and scans the result into the given value.
func (uavgb *UpgradeApkVersionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uavgb.build.ctx, ent.OpQueryGroupBy)
	if err := uavgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UpgradeApkVersionQuery, *UpgradeApkVersionGroupBy](ctx, uavgb.build, uavgb, uavgb.build.inters, v)
}

func (uavgb *UpgradeApkVersionGroupBy) sqlScan(ctx context.Context, root *UpgradeApkVersionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(uavgb.fns))
	for _, fn := range uavgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*uavgb.flds)+len(uavgb.fns))
		for _, f := range *uavgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*uavgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uavgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UpgradeApkVersionSelect is the builder for selecting fields of UpgradeApkVersion entities.
type UpgradeApkVersionSelect struct {
	*UpgradeApkVersionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (uavs *UpgradeApkVersionSelect) Aggregate(fns ...AggregateFunc) *UpgradeApkVersionSelect {
	uavs.fns = append(uavs.fns, fns...)
	return uavs
}

// Scan applies the selector query and scans the result into the given value.
func (uavs *UpgradeApkVersionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uavs.ctx, ent.OpQuerySelect)
	if err := uavs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UpgradeApkVersionQuery, *UpgradeApkVersionSelect](ctx, uavs.UpgradeApkVersionQuery, uavs, uavs.inters, v)
}

func (uavs *UpgradeApkVersionSelect) sqlScan(ctx context.Context, root *UpgradeApkVersionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(uavs.fns))
	for _, fn := range uavs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*uavs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uavs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
