// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"upgradelink-admin-upgrade/server/ent/predicate"
	"upgradelink-admin-upgrade/server/ent/upgradedevgroup"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UpgradeDevGroupQuery is the builder for querying UpgradeDevGroup entities.
type UpgradeDevGroupQuery struct {
	config
	ctx        *QueryContext
	order      []upgradedevgroup.OrderOption
	inters     []Interceptor
	predicates []predicate.UpgradeDevGroup
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UpgradeDevGroupQuery builder.
func (udgq *UpgradeDevGroupQuery) Where(ps ...predicate.UpgradeDevGroup) *UpgradeDevGroupQuery {
	udgq.predicates = append(udgq.predicates, ps...)
	return udgq
}

// Limit the number of records to be returned by this query.
func (udgq *UpgradeDevGroupQuery) Limit(limit int) *UpgradeDevGroupQuery {
	udgq.ctx.Limit = &limit
	return udgq
}

// Offset to start from.
func (udgq *UpgradeDevGroupQuery) Offset(offset int) *UpgradeDevGroupQuery {
	udgq.ctx.Offset = &offset
	return udgq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (udgq *UpgradeDevGroupQuery) Unique(unique bool) *UpgradeDevGroupQuery {
	udgq.ctx.Unique = &unique
	return udgq
}

// Order specifies how the records should be ordered.
func (udgq *UpgradeDevGroupQuery) Order(o ...upgradedevgroup.OrderOption) *UpgradeDevGroupQuery {
	udgq.order = append(udgq.order, o...)
	return udgq
}

// First returns the first UpgradeDevGroup entity from the query.
// Returns a *NotFoundError when no UpgradeDevGroup was found.
func (udgq *UpgradeDevGroupQuery) First(ctx context.Context) (*UpgradeDevGroup, error) {
	nodes, err := udgq.Limit(1).All(setContextOp(ctx, udgq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{upgradedevgroup.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (udgq *UpgradeDevGroupQuery) FirstX(ctx context.Context) *UpgradeDevGroup {
	node, err := udgq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UpgradeDevGroup ID from the query.
// Returns a *NotFoundError when no UpgradeDevGroup ID was found.
func (udgq *UpgradeDevGroupQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = udgq.Limit(1).IDs(setContextOp(ctx, udgq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{upgradedevgroup.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (udgq *UpgradeDevGroupQuery) FirstIDX(ctx context.Context) int {
	id, err := udgq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UpgradeDevGroup entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UpgradeDevGroup entity is found.
// Returns a *NotFoundError when no UpgradeDevGroup entities are found.
func (udgq *UpgradeDevGroupQuery) Only(ctx context.Context) (*UpgradeDevGroup, error) {
	nodes, err := udgq.Limit(2).All(setContextOp(ctx, udgq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{upgradedevgroup.Label}
	default:
		return nil, &NotSingularError{upgradedevgroup.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (udgq *UpgradeDevGroupQuery) OnlyX(ctx context.Context) *UpgradeDevGroup {
	node, err := udgq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UpgradeDevGroup ID in the query.
// Returns a *NotSingularError when more than one UpgradeDevGroup ID is found.
// Returns a *NotFoundError when no entities are found.
func (udgq *UpgradeDevGroupQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = udgq.Limit(2).IDs(setContextOp(ctx, udgq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{upgradedevgroup.Label}
	default:
		err = &NotSingularError{upgradedevgroup.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (udgq *UpgradeDevGroupQuery) OnlyIDX(ctx context.Context) int {
	id, err := udgq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UpgradeDevGroups.
func (udgq *UpgradeDevGroupQuery) All(ctx context.Context) ([]*UpgradeDevGroup, error) {
	ctx = setContextOp(ctx, udgq.ctx, ent.OpQueryAll)
	if err := udgq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UpgradeDevGroup, *UpgradeDevGroupQuery]()
	return withInterceptors[[]*UpgradeDevGroup](ctx, udgq, qr, udgq.inters)
}

// AllX is like All, but panics if an error occurs.
func (udgq *UpgradeDevGroupQuery) AllX(ctx context.Context) []*UpgradeDevGroup {
	nodes, err := udgq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UpgradeDevGroup IDs.
func (udgq *UpgradeDevGroupQuery) IDs(ctx context.Context) (ids []int, err error) {
	if udgq.ctx.Unique == nil && udgq.path != nil {
		udgq.Unique(true)
	}
	ctx = setContextOp(ctx, udgq.ctx, ent.OpQueryIDs)
	if err = udgq.Select(upgradedevgroup.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (udgq *UpgradeDevGroupQuery) IDsX(ctx context.Context) []int {
	ids, err := udgq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (udgq *UpgradeDevGroupQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, udgq.ctx, ent.OpQueryCount)
	if err := udgq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, udgq, querierCount[*UpgradeDevGroupQuery](), udgq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (udgq *UpgradeDevGroupQuery) CountX(ctx context.Context) int {
	count, err := udgq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (udgq *UpgradeDevGroupQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, udgq.ctx, ent.OpQueryExist)
	switch _, err := udgq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (udgq *UpgradeDevGroupQuery) ExistX(ctx context.Context) bool {
	exist, err := udgq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UpgradeDevGroupQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (udgq *UpgradeDevGroupQuery) Clone() *UpgradeDevGroupQuery {
	if udgq == nil {
		return nil
	}
	return &UpgradeDevGroupQuery{
		config:     udgq.config,
		ctx:        udgq.ctx.Clone(),
		order:      append([]upgradedevgroup.OrderOption{}, udgq.order...),
		inters:     append([]Interceptor{}, udgq.inters...),
		predicates: append([]predicate.UpgradeDevGroup{}, udgq.predicates...),
		// clone intermediate query.
		sql:  udgq.sql.Clone(),
		path: udgq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CompanyID int `json:"company_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UpgradeDevGroup.Query().
//		GroupBy(upgradedevgroup.FieldCompanyID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (udgq *UpgradeDevGroupQuery) GroupBy(field string, fields ...string) *UpgradeDevGroupGroupBy {
	udgq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UpgradeDevGroupGroupBy{build: udgq}
	grbuild.flds = &udgq.ctx.Fields
	grbuild.label = upgradedevgroup.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CompanyID int `json:"company_id,omitempty"`
//	}
//
//	client.UpgradeDevGroup.Query().
//		Select(upgradedevgroup.FieldCompanyID).
//		Scan(ctx, &v)
func (udgq *UpgradeDevGroupQuery) Select(fields ...string) *UpgradeDevGroupSelect {
	udgq.ctx.Fields = append(udgq.ctx.Fields, fields...)
	sbuild := &UpgradeDevGroupSelect{UpgradeDevGroupQuery: udgq}
	sbuild.label = upgradedevgroup.Label
	sbuild.flds, sbuild.scan = &udgq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UpgradeDevGroupSelect configured with the given aggregations.
func (udgq *UpgradeDevGroupQuery) Aggregate(fns ...AggregateFunc) *UpgradeDevGroupSelect {
	return udgq.Select().Aggregate(fns...)
}

func (udgq *UpgradeDevGroupQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range udgq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, udgq); err != nil {
				return err
			}
		}
	}
	for _, f := range udgq.ctx.Fields {
		if !upgradedevgroup.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if udgq.path != nil {
		prev, err := udgq.path(ctx)
		if err != nil {
			return err
		}
		udgq.sql = prev
	}
	return nil
}

func (udgq *UpgradeDevGroupQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UpgradeDevGroup, error) {
	var (
		nodes = []*UpgradeDevGroup{}
		_spec = udgq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UpgradeDevGroup).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UpgradeDevGroup{config: udgq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, udgq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (udgq *UpgradeDevGroupQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := udgq.querySpec()
	_spec.Node.Columns = udgq.ctx.Fields
	if len(udgq.ctx.Fields) > 0 {
		_spec.Unique = udgq.ctx.Unique != nil && *udgq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, udgq.driver, _spec)
}

func (udgq *UpgradeDevGroupQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(upgradedevgroup.Table, upgradedevgroup.Columns, sqlgraph.NewFieldSpec(upgradedevgroup.FieldID, field.TypeInt))
	_spec.From = udgq.sql
	if unique := udgq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if udgq.path != nil {
		_spec.Unique = true
	}
	if fields := udgq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, upgradedevgroup.FieldID)
		for i := range fields {
			if fields[i] != upgradedevgroup.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := udgq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := udgq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := udgq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := udgq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (udgq *UpgradeDevGroupQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(udgq.driver.Dialect())
	t1 := builder.Table(upgradedevgroup.Table)
	columns := udgq.ctx.Fields
	if len(columns) == 0 {
		columns = upgradedevgroup.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if udgq.sql != nil {
		selector = udgq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if udgq.ctx.Unique != nil && *udgq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range udgq.predicates {
		p(selector)
	}
	for _, p := range udgq.order {
		p(selector)
	}
	if offset := udgq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := udgq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UpgradeDevGroupGroupBy is the group-by builder for UpgradeDevGroup entities.
type UpgradeDevGroupGroupBy struct {
	selector
	build *UpgradeDevGroupQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (udggb *UpgradeDevGroupGroupBy) Aggregate(fns ...AggregateFunc) *UpgradeDevGroupGroupBy {
	udggb.fns = append(udggb.fns, fns...)
	return udggb
}

// Scan applies the selector query and scans the result into the given value.
func (udggb *UpgradeDevGroupGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, udggb.build.ctx, ent.OpQueryGroupBy)
	if err := udggb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UpgradeDevGroupQuery, *UpgradeDevGroupGroupBy](ctx, udggb.build, udggb, udggb.build.inters, v)
}

func (udggb *UpgradeDevGroupGroupBy) sqlScan(ctx context.Context, root *UpgradeDevGroupQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(udggb.fns))
	for _, fn := range udggb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*udggb.flds)+len(udggb.fns))
		for _, f := range *udggb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*udggb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := udggb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UpgradeDevGroupSelect is the builder for selecting fields of UpgradeDevGroup entities.
type UpgradeDevGroupSelect struct {
	*UpgradeDevGroupQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (udgs *UpgradeDevGroupSelect) Aggregate(fns ...AggregateFunc) *UpgradeDevGroupSelect {
	udgs.fns = append(udgs.fns, fns...)
	return udgs
}

// Scan applies the selector query and scans the result into the given value.
func (udgs *UpgradeDevGroupSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, udgs.ctx, ent.OpQuerySelect)
	if err := udgs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UpgradeDevGroupQuery, *UpgradeDevGroupSelect](ctx, udgs.UpgradeDevGroupQuery, udgs, udgs.inters, v)
}

func (udgs *UpgradeDevGroupSelect) sqlScan(ctx context.Context, root *UpgradeDevGroupQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(udgs.fns))
	for _, fn := range udgs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*udgs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := udgs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
