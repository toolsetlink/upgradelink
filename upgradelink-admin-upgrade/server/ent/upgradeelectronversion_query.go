// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"upgradelink-admin-upgrade/server/ent/predicate"
	"upgradelink-admin-upgrade/server/ent/upgradeelectronversion"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UpgradeElectronVersionQuery is the builder for querying UpgradeElectronVersion entities.
type UpgradeElectronVersionQuery struct {
	config
	ctx        *QueryContext
	order      []upgradeelectronversion.OrderOption
	inters     []Interceptor
	predicates []predicate.UpgradeElectronVersion
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UpgradeElectronVersionQuery builder.
func (uevq *UpgradeElectronVersionQuery) Where(ps ...predicate.UpgradeElectronVersion) *UpgradeElectronVersionQuery {
	uevq.predicates = append(uevq.predicates, ps...)
	return uevq
}

// Limit the number of records to be returned by this query.
func (uevq *UpgradeElectronVersionQuery) Limit(limit int) *UpgradeElectronVersionQuery {
	uevq.ctx.Limit = &limit
	return uevq
}

// Offset to start from.
func (uevq *UpgradeElectronVersionQuery) Offset(offset int) *UpgradeElectronVersionQuery {
	uevq.ctx.Offset = &offset
	return uevq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (uevq *UpgradeElectronVersionQuery) Unique(unique bool) *UpgradeElectronVersionQuery {
	uevq.ctx.Unique = &unique
	return uevq
}

// Order specifies how the records should be ordered.
func (uevq *UpgradeElectronVersionQuery) Order(o ...upgradeelectronversion.OrderOption) *UpgradeElectronVersionQuery {
	uevq.order = append(uevq.order, o...)
	return uevq
}

// First returns the first UpgradeElectronVersion entity from the query.
// Returns a *NotFoundError when no UpgradeElectronVersion was found.
func (uevq *UpgradeElectronVersionQuery) First(ctx context.Context) (*UpgradeElectronVersion, error) {
	nodes, err := uevq.Limit(1).All(setContextOp(ctx, uevq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{upgradeelectronversion.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (uevq *UpgradeElectronVersionQuery) FirstX(ctx context.Context) *UpgradeElectronVersion {
	node, err := uevq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UpgradeElectronVersion ID from the query.
// Returns a *NotFoundError when no UpgradeElectronVersion ID was found.
func (uevq *UpgradeElectronVersionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uevq.Limit(1).IDs(setContextOp(ctx, uevq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{upgradeelectronversion.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (uevq *UpgradeElectronVersionQuery) FirstIDX(ctx context.Context) int {
	id, err := uevq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UpgradeElectronVersion entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UpgradeElectronVersion entity is found.
// Returns a *NotFoundError when no UpgradeElectronVersion entities are found.
func (uevq *UpgradeElectronVersionQuery) Only(ctx context.Context) (*UpgradeElectronVersion, error) {
	nodes, err := uevq.Limit(2).All(setContextOp(ctx, uevq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{upgradeelectronversion.Label}
	default:
		return nil, &NotSingularError{upgradeelectronversion.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (uevq *UpgradeElectronVersionQuery) OnlyX(ctx context.Context) *UpgradeElectronVersion {
	node, err := uevq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UpgradeElectronVersion ID in the query.
// Returns a *NotSingularError when more than one UpgradeElectronVersion ID is found.
// Returns a *NotFoundError when no entities are found.
func (uevq *UpgradeElectronVersionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uevq.Limit(2).IDs(setContextOp(ctx, uevq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{upgradeelectronversion.Label}
	default:
		err = &NotSingularError{upgradeelectronversion.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (uevq *UpgradeElectronVersionQuery) OnlyIDX(ctx context.Context) int {
	id, err := uevq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UpgradeElectronVersions.
func (uevq *UpgradeElectronVersionQuery) All(ctx context.Context) ([]*UpgradeElectronVersion, error) {
	ctx = setContextOp(ctx, uevq.ctx, ent.OpQueryAll)
	if err := uevq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UpgradeElectronVersion, *UpgradeElectronVersionQuery]()
	return withInterceptors[[]*UpgradeElectronVersion](ctx, uevq, qr, uevq.inters)
}

// AllX is like All, but panics if an error occurs.
func (uevq *UpgradeElectronVersionQuery) AllX(ctx context.Context) []*UpgradeElectronVersion {
	nodes, err := uevq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UpgradeElectronVersion IDs.
func (uevq *UpgradeElectronVersionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if uevq.ctx.Unique == nil && uevq.path != nil {
		uevq.Unique(true)
	}
	ctx = setContextOp(ctx, uevq.ctx, ent.OpQueryIDs)
	if err = uevq.Select(upgradeelectronversion.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (uevq *UpgradeElectronVersionQuery) IDsX(ctx context.Context) []int {
	ids, err := uevq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (uevq *UpgradeElectronVersionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, uevq.ctx, ent.OpQueryCount)
	if err := uevq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, uevq, querierCount[*UpgradeElectronVersionQuery](), uevq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (uevq *UpgradeElectronVersionQuery) CountX(ctx context.Context) int {
	count, err := uevq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (uevq *UpgradeElectronVersionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, uevq.ctx, ent.OpQueryExist)
	switch _, err := uevq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (uevq *UpgradeElectronVersionQuery) ExistX(ctx context.Context) bool {
	exist, err := uevq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UpgradeElectronVersionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (uevq *UpgradeElectronVersionQuery) Clone() *UpgradeElectronVersionQuery {
	if uevq == nil {
		return nil
	}
	return &UpgradeElectronVersionQuery{
		config:     uevq.config,
		ctx:        uevq.ctx.Clone(),
		order:      append([]upgradeelectronversion.OrderOption{}, uevq.order...),
		inters:     append([]Interceptor{}, uevq.inters...),
		predicates: append([]predicate.UpgradeElectronVersion{}, uevq.predicates...),
		// clone intermediate query.
		sql:  uevq.sql.Clone(),
		path: uevq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CompanyID int `json:"company_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UpgradeElectronVersion.Query().
//		GroupBy(upgradeelectronversion.FieldCompanyID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (uevq *UpgradeElectronVersionQuery) GroupBy(field string, fields ...string) *UpgradeElectronVersionGroupBy {
	uevq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UpgradeElectronVersionGroupBy{build: uevq}
	grbuild.flds = &uevq.ctx.Fields
	grbuild.label = upgradeelectronversion.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CompanyID int `json:"company_id,omitempty"`
//	}
//
//	client.UpgradeElectronVersion.Query().
//		Select(upgradeelectronversion.FieldCompanyID).
//		Scan(ctx, &v)
func (uevq *UpgradeElectronVersionQuery) Select(fields ...string) *UpgradeElectronVersionSelect {
	uevq.ctx.Fields = append(uevq.ctx.Fields, fields...)
	sbuild := &UpgradeElectronVersionSelect{UpgradeElectronVersionQuery: uevq}
	sbuild.label = upgradeelectronversion.Label
	sbuild.flds, sbuild.scan = &uevq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UpgradeElectronVersionSelect configured with the given aggregations.
func (uevq *UpgradeElectronVersionQuery) Aggregate(fns ...AggregateFunc) *UpgradeElectronVersionSelect {
	return uevq.Select().Aggregate(fns...)
}

func (uevq *UpgradeElectronVersionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range uevq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, uevq); err != nil {
				return err
			}
		}
	}
	for _, f := range uevq.ctx.Fields {
		if !upgradeelectronversion.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if uevq.path != nil {
		prev, err := uevq.path(ctx)
		if err != nil {
			return err
		}
		uevq.sql = prev
	}
	return nil
}

func (uevq *UpgradeElectronVersionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UpgradeElectronVersion, error) {
	var (
		nodes = []*UpgradeElectronVersion{}
		_spec = uevq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UpgradeElectronVersion).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UpgradeElectronVersion{config: uevq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, uevq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (uevq *UpgradeElectronVersionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := uevq.querySpec()
	_spec.Node.Columns = uevq.ctx.Fields
	if len(uevq.ctx.Fields) > 0 {
		_spec.Unique = uevq.ctx.Unique != nil && *uevq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, uevq.driver, _spec)
}

func (uevq *UpgradeElectronVersionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(upgradeelectronversion.Table, upgradeelectronversion.Columns, sqlgraph.NewFieldSpec(upgradeelectronversion.FieldID, field.TypeInt))
	_spec.From = uevq.sql
	if unique := uevq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if uevq.path != nil {
		_spec.Unique = true
	}
	if fields := uevq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, upgradeelectronversion.FieldID)
		for i := range fields {
			if fields[i] != upgradeelectronversion.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := uevq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := uevq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := uevq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := uevq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (uevq *UpgradeElectronVersionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(uevq.driver.Dialect())
	t1 := builder.Table(upgradeelectronversion.Table)
	columns := uevq.ctx.Fields
	if len(columns) == 0 {
		columns = upgradeelectronversion.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if uevq.sql != nil {
		selector = uevq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if uevq.ctx.Unique != nil && *uevq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range uevq.predicates {
		p(selector)
	}
	for _, p := range uevq.order {
		p(selector)
	}
	if offset := uevq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := uevq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UpgradeElectronVersionGroupBy is the group-by builder for UpgradeElectronVersion entities.
type UpgradeElectronVersionGroupBy struct {
	selector
	build *UpgradeElectronVersionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (uevgb *UpgradeElectronVersionGroupBy) Aggregate(fns ...AggregateFunc) *UpgradeElectronVersionGroupBy {
	uevgb.fns = append(uevgb.fns, fns...)
	return uevgb
}

// Scan applies the selector query and scans the result into the given value.
func (uevgb *UpgradeElectronVersionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uevgb.build.ctx, ent.OpQueryGroupBy)
	if err := uevgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UpgradeElectronVersionQuery, *UpgradeElectronVersionGroupBy](ctx, uevgb.build, uevgb, uevgb.build.inters, v)
}

func (uevgb *UpgradeElectronVersionGroupBy) sqlScan(ctx context.Context, root *UpgradeElectronVersionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(uevgb.fns))
	for _, fn := range uevgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*uevgb.flds)+len(uevgb.fns))
		for _, f := range *uevgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*uevgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uevgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UpgradeElectronVersionSelect is the builder for selecting fields of UpgradeElectronVersion entities.
type UpgradeElectronVersionSelect struct {
	*UpgradeElectronVersionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (uevs *UpgradeElectronVersionSelect) Aggregate(fns ...AggregateFunc) *UpgradeElectronVersionSelect {
	uevs.fns = append(uevs.fns, fns...)
	return uevs
}

// Scan applies the selector query and scans the result into the given value.
func (uevs *UpgradeElectronVersionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uevs.ctx, ent.OpQuerySelect)
	if err := uevs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UpgradeElectronVersionQuery, *UpgradeElectronVersionSelect](ctx, uevs.UpgradeElectronVersionQuery, uevs, uevs.inters, v)
}

func (uevs *UpgradeElectronVersionSelect) sqlScan(ctx context.Context, root *UpgradeElectronVersionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(uevs.fns))
	for _, fn := range uevs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*uevs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uevs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
