// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"upgradelink-admin-upgrade/server/ent/predicate"
	"upgradelink-admin-upgrade/server/ent/upgradeurlversion"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UpgradeUrlVersionQuery is the builder for querying UpgradeUrlVersion entities.
type UpgradeUrlVersionQuery struct {
	config
	ctx        *QueryContext
	order      []upgradeurlversion.OrderOption
	inters     []Interceptor
	predicates []predicate.UpgradeUrlVersion
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UpgradeUrlVersionQuery builder.
func (uuvq *UpgradeUrlVersionQuery) Where(ps ...predicate.UpgradeUrlVersion) *UpgradeUrlVersionQuery {
	uuvq.predicates = append(uuvq.predicates, ps...)
	return uuvq
}

// Limit the number of records to be returned by this query.
func (uuvq *UpgradeUrlVersionQuery) Limit(limit int) *UpgradeUrlVersionQuery {
	uuvq.ctx.Limit = &limit
	return uuvq
}

// Offset to start from.
func (uuvq *UpgradeUrlVersionQuery) Offset(offset int) *UpgradeUrlVersionQuery {
	uuvq.ctx.Offset = &offset
	return uuvq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (uuvq *UpgradeUrlVersionQuery) Unique(unique bool) *UpgradeUrlVersionQuery {
	uuvq.ctx.Unique = &unique
	return uuvq
}

// Order specifies how the records should be ordered.
func (uuvq *UpgradeUrlVersionQuery) Order(o ...upgradeurlversion.OrderOption) *UpgradeUrlVersionQuery {
	uuvq.order = append(uuvq.order, o...)
	return uuvq
}

// First returns the first UpgradeUrlVersion entity from the query.
// Returns a *NotFoundError when no UpgradeUrlVersion was found.
func (uuvq *UpgradeUrlVersionQuery) First(ctx context.Context) (*UpgradeUrlVersion, error) {
	nodes, err := uuvq.Limit(1).All(setContextOp(ctx, uuvq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{upgradeurlversion.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (uuvq *UpgradeUrlVersionQuery) FirstX(ctx context.Context) *UpgradeUrlVersion {
	node, err := uuvq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UpgradeUrlVersion ID from the query.
// Returns a *NotFoundError when no UpgradeUrlVersion ID was found.
func (uuvq *UpgradeUrlVersionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uuvq.Limit(1).IDs(setContextOp(ctx, uuvq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{upgradeurlversion.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (uuvq *UpgradeUrlVersionQuery) FirstIDX(ctx context.Context) int {
	id, err := uuvq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UpgradeUrlVersion entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UpgradeUrlVersion entity is found.
// Returns a *NotFoundError when no UpgradeUrlVersion entities are found.
func (uuvq *UpgradeUrlVersionQuery) Only(ctx context.Context) (*UpgradeUrlVersion, error) {
	nodes, err := uuvq.Limit(2).All(setContextOp(ctx, uuvq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{upgradeurlversion.Label}
	default:
		return nil, &NotSingularError{upgradeurlversion.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (uuvq *UpgradeUrlVersionQuery) OnlyX(ctx context.Context) *UpgradeUrlVersion {
	node, err := uuvq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UpgradeUrlVersion ID in the query.
// Returns a *NotSingularError when more than one UpgradeUrlVersion ID is found.
// Returns a *NotFoundError when no entities are found.
func (uuvq *UpgradeUrlVersionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uuvq.Limit(2).IDs(setContextOp(ctx, uuvq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{upgradeurlversion.Label}
	default:
		err = &NotSingularError{upgradeurlversion.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (uuvq *UpgradeUrlVersionQuery) OnlyIDX(ctx context.Context) int {
	id, err := uuvq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UpgradeUrlVersions.
func (uuvq *UpgradeUrlVersionQuery) All(ctx context.Context) ([]*UpgradeUrlVersion, error) {
	ctx = setContextOp(ctx, uuvq.ctx, ent.OpQueryAll)
	if err := uuvq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UpgradeUrlVersion, *UpgradeUrlVersionQuery]()
	return withInterceptors[[]*UpgradeUrlVersion](ctx, uuvq, qr, uuvq.inters)
}

// AllX is like All, but panics if an error occurs.
func (uuvq *UpgradeUrlVersionQuery) AllX(ctx context.Context) []*UpgradeUrlVersion {
	nodes, err := uuvq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UpgradeUrlVersion IDs.
func (uuvq *UpgradeUrlVersionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if uuvq.ctx.Unique == nil && uuvq.path != nil {
		uuvq.Unique(true)
	}
	ctx = setContextOp(ctx, uuvq.ctx, ent.OpQueryIDs)
	if err = uuvq.Select(upgradeurlversion.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (uuvq *UpgradeUrlVersionQuery) IDsX(ctx context.Context) []int {
	ids, err := uuvq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (uuvq *UpgradeUrlVersionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, uuvq.ctx, ent.OpQueryCount)
	if err := uuvq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, uuvq, querierCount[*UpgradeUrlVersionQuery](), uuvq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (uuvq *UpgradeUrlVersionQuery) CountX(ctx context.Context) int {
	count, err := uuvq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (uuvq *UpgradeUrlVersionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, uuvq.ctx, ent.OpQueryExist)
	switch _, err := uuvq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (uuvq *UpgradeUrlVersionQuery) ExistX(ctx context.Context) bool {
	exist, err := uuvq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UpgradeUrlVersionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (uuvq *UpgradeUrlVersionQuery) Clone() *UpgradeUrlVersionQuery {
	if uuvq == nil {
		return nil
	}
	return &UpgradeUrlVersionQuery{
		config:     uuvq.config,
		ctx:        uuvq.ctx.Clone(),
		order:      append([]upgradeurlversion.OrderOption{}, uuvq.order...),
		inters:     append([]Interceptor{}, uuvq.inters...),
		predicates: append([]predicate.UpgradeUrlVersion{}, uuvq.predicates...),
		// clone intermediate query.
		sql:  uuvq.sql.Clone(),
		path: uuvq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CompanyID int `json:"company_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UpgradeUrlVersion.Query().
//		GroupBy(upgradeurlversion.FieldCompanyID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (uuvq *UpgradeUrlVersionQuery) GroupBy(field string, fields ...string) *UpgradeUrlVersionGroupBy {
	uuvq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UpgradeUrlVersionGroupBy{build: uuvq}
	grbuild.flds = &uuvq.ctx.Fields
	grbuild.label = upgradeurlversion.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CompanyID int `json:"company_id,omitempty"`
//	}
//
//	client.UpgradeUrlVersion.Query().
//		Select(upgradeurlversion.FieldCompanyID).
//		Scan(ctx, &v)
func (uuvq *UpgradeUrlVersionQuery) Select(fields ...string) *UpgradeUrlVersionSelect {
	uuvq.ctx.Fields = append(uuvq.ctx.Fields, fields...)
	sbuild := &UpgradeUrlVersionSelect{UpgradeUrlVersionQuery: uuvq}
	sbuild.label = upgradeurlversion.Label
	sbuild.flds, sbuild.scan = &uuvq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UpgradeUrlVersionSelect configured with the given aggregations.
func (uuvq *UpgradeUrlVersionQuery) Aggregate(fns ...AggregateFunc) *UpgradeUrlVersionSelect {
	return uuvq.Select().Aggregate(fns...)
}

func (uuvq *UpgradeUrlVersionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range uuvq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, uuvq); err != nil {
				return err
			}
		}
	}
	for _, f := range uuvq.ctx.Fields {
		if !upgradeurlversion.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if uuvq.path != nil {
		prev, err := uuvq.path(ctx)
		if err != nil {
			return err
		}
		uuvq.sql = prev
	}
	return nil
}

func (uuvq *UpgradeUrlVersionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UpgradeUrlVersion, error) {
	var (
		nodes = []*UpgradeUrlVersion{}
		_spec = uuvq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UpgradeUrlVersion).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UpgradeUrlVersion{config: uuvq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, uuvq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (uuvq *UpgradeUrlVersionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := uuvq.querySpec()
	_spec.Node.Columns = uuvq.ctx.Fields
	if len(uuvq.ctx.Fields) > 0 {
		_spec.Unique = uuvq.ctx.Unique != nil && *uuvq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, uuvq.driver, _spec)
}

func (uuvq *UpgradeUrlVersionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(upgradeurlversion.Table, upgradeurlversion.Columns, sqlgraph.NewFieldSpec(upgradeurlversion.FieldID, field.TypeInt))
	_spec.From = uuvq.sql
	if unique := uuvq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if uuvq.path != nil {
		_spec.Unique = true
	}
	if fields := uuvq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, upgradeurlversion.FieldID)
		for i := range fields {
			if fields[i] != upgradeurlversion.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := uuvq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := uuvq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := uuvq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := uuvq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (uuvq *UpgradeUrlVersionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(uuvq.driver.Dialect())
	t1 := builder.Table(upgradeurlversion.Table)
	columns := uuvq.ctx.Fields
	if len(columns) == 0 {
		columns = upgradeurlversion.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if uuvq.sql != nil {
		selector = uuvq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if uuvq.ctx.Unique != nil && *uuvq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range uuvq.predicates {
		p(selector)
	}
	for _, p := range uuvq.order {
		p(selector)
	}
	if offset := uuvq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := uuvq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UpgradeUrlVersionGroupBy is the group-by builder for UpgradeUrlVersion entities.
type UpgradeUrlVersionGroupBy struct {
	selector
	build *UpgradeUrlVersionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (uuvgb *UpgradeUrlVersionGroupBy) Aggregate(fns ...AggregateFunc) *UpgradeUrlVersionGroupBy {
	uuvgb.fns = append(uuvgb.fns, fns...)
	return uuvgb
}

// Scan applies the selector query and scans the result into the given value.
func (uuvgb *UpgradeUrlVersionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uuvgb.build.ctx, ent.OpQueryGroupBy)
	if err := uuvgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UpgradeUrlVersionQuery, *UpgradeUrlVersionGroupBy](ctx, uuvgb.build, uuvgb, uuvgb.build.inters, v)
}

func (uuvgb *UpgradeUrlVersionGroupBy) sqlScan(ctx context.Context, root *UpgradeUrlVersionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(uuvgb.fns))
	for _, fn := range uuvgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*uuvgb.flds)+len(uuvgb.fns))
		for _, f := range *uuvgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*uuvgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uuvgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UpgradeUrlVersionSelect is the builder for selecting fields of UpgradeUrlVersion entities.
type UpgradeUrlVersionSelect struct {
	*UpgradeUrlVersionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (uuvs *UpgradeUrlVersionSelect) Aggregate(fns ...AggregateFunc) *UpgradeUrlVersionSelect {
	uuvs.fns = append(uuvs.fns, fns...)
	return uuvs
}

// Scan applies the selector query and scans the result into the given value.
func (uuvs *UpgradeUrlVersionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uuvs.ctx, ent.OpQuerySelect)
	if err := uuvs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UpgradeUrlVersionQuery, *UpgradeUrlVersionSelect](ctx, uuvs.UpgradeUrlVersionQuery, uuvs, uuvs.inters, v)
}

func (uuvs *UpgradeUrlVersionSelect) sqlScan(ctx context.Context, root *UpgradeUrlVersionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(uuvs.fns))
	for _, fn := range uuvs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*uuvs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uuvs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
