// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"upgradelink-admin-upgrade/server/ent/predicate"
	"upgradelink-admin-upgrade/server/ent/upgradedevmodel"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UpgradeDevModelQuery is the builder for querying UpgradeDevModel entities.
type UpgradeDevModelQuery struct {
	config
	ctx        *QueryContext
	order      []upgradedevmodel.OrderOption
	inters     []Interceptor
	predicates []predicate.UpgradeDevModel
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UpgradeDevModelQuery builder.
func (udmq *UpgradeDevModelQuery) Where(ps ...predicate.UpgradeDevModel) *UpgradeDevModelQuery {
	udmq.predicates = append(udmq.predicates, ps...)
	return udmq
}

// Limit the number of records to be returned by this query.
func (udmq *UpgradeDevModelQuery) Limit(limit int) *UpgradeDevModelQuery {
	udmq.ctx.Limit = &limit
	return udmq
}

// Offset to start from.
func (udmq *UpgradeDevModelQuery) Offset(offset int) *UpgradeDevModelQuery {
	udmq.ctx.Offset = &offset
	return udmq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (udmq *UpgradeDevModelQuery) Unique(unique bool) *UpgradeDevModelQuery {
	udmq.ctx.Unique = &unique
	return udmq
}

// Order specifies how the records should be ordered.
func (udmq *UpgradeDevModelQuery) Order(o ...upgradedevmodel.OrderOption) *UpgradeDevModelQuery {
	udmq.order = append(udmq.order, o...)
	return udmq
}

// First returns the first UpgradeDevModel entity from the query.
// Returns a *NotFoundError when no UpgradeDevModel was found.
func (udmq *UpgradeDevModelQuery) First(ctx context.Context) (*UpgradeDevModel, error) {
	nodes, err := udmq.Limit(1).All(setContextOp(ctx, udmq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{upgradedevmodel.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (udmq *UpgradeDevModelQuery) FirstX(ctx context.Context) *UpgradeDevModel {
	node, err := udmq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UpgradeDevModel ID from the query.
// Returns a *NotFoundError when no UpgradeDevModel ID was found.
func (udmq *UpgradeDevModelQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = udmq.Limit(1).IDs(setContextOp(ctx, udmq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{upgradedevmodel.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (udmq *UpgradeDevModelQuery) FirstIDX(ctx context.Context) int {
	id, err := udmq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UpgradeDevModel entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UpgradeDevModel entity is found.
// Returns a *NotFoundError when no UpgradeDevModel entities are found.
func (udmq *UpgradeDevModelQuery) Only(ctx context.Context) (*UpgradeDevModel, error) {
	nodes, err := udmq.Limit(2).All(setContextOp(ctx, udmq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{upgradedevmodel.Label}
	default:
		return nil, &NotSingularError{upgradedevmodel.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (udmq *UpgradeDevModelQuery) OnlyX(ctx context.Context) *UpgradeDevModel {
	node, err := udmq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UpgradeDevModel ID in the query.
// Returns a *NotSingularError when more than one UpgradeDevModel ID is found.
// Returns a *NotFoundError when no entities are found.
func (udmq *UpgradeDevModelQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = udmq.Limit(2).IDs(setContextOp(ctx, udmq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{upgradedevmodel.Label}
	default:
		err = &NotSingularError{upgradedevmodel.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (udmq *UpgradeDevModelQuery) OnlyIDX(ctx context.Context) int {
	id, err := udmq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UpgradeDevModels.
func (udmq *UpgradeDevModelQuery) All(ctx context.Context) ([]*UpgradeDevModel, error) {
	ctx = setContextOp(ctx, udmq.ctx, ent.OpQueryAll)
	if err := udmq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UpgradeDevModel, *UpgradeDevModelQuery]()
	return withInterceptors[[]*UpgradeDevModel](ctx, udmq, qr, udmq.inters)
}

// AllX is like All, but panics if an error occurs.
func (udmq *UpgradeDevModelQuery) AllX(ctx context.Context) []*UpgradeDevModel {
	nodes, err := udmq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UpgradeDevModel IDs.
func (udmq *UpgradeDevModelQuery) IDs(ctx context.Context) (ids []int, err error) {
	if udmq.ctx.Unique == nil && udmq.path != nil {
		udmq.Unique(true)
	}
	ctx = setContextOp(ctx, udmq.ctx, ent.OpQueryIDs)
	if err = udmq.Select(upgradedevmodel.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (udmq *UpgradeDevModelQuery) IDsX(ctx context.Context) []int {
	ids, err := udmq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (udmq *UpgradeDevModelQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, udmq.ctx, ent.OpQueryCount)
	if err := udmq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, udmq, querierCount[*UpgradeDevModelQuery](), udmq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (udmq *UpgradeDevModelQuery) CountX(ctx context.Context) int {
	count, err := udmq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (udmq *UpgradeDevModelQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, udmq.ctx, ent.OpQueryExist)
	switch _, err := udmq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (udmq *UpgradeDevModelQuery) ExistX(ctx context.Context) bool {
	exist, err := udmq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UpgradeDevModelQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (udmq *UpgradeDevModelQuery) Clone() *UpgradeDevModelQuery {
	if udmq == nil {
		return nil
	}
	return &UpgradeDevModelQuery{
		config:     udmq.config,
		ctx:        udmq.ctx.Clone(),
		order:      append([]upgradedevmodel.OrderOption{}, udmq.order...),
		inters:     append([]Interceptor{}, udmq.inters...),
		predicates: append([]predicate.UpgradeDevModel{}, udmq.predicates...),
		// clone intermediate query.
		sql:  udmq.sql.Clone(),
		path: udmq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CompanyID int `json:"company_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UpgradeDevModel.Query().
//		GroupBy(upgradedevmodel.FieldCompanyID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (udmq *UpgradeDevModelQuery) GroupBy(field string, fields ...string) *UpgradeDevModelGroupBy {
	udmq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UpgradeDevModelGroupBy{build: udmq}
	grbuild.flds = &udmq.ctx.Fields
	grbuild.label = upgradedevmodel.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CompanyID int `json:"company_id,omitempty"`
//	}
//
//	client.UpgradeDevModel.Query().
//		Select(upgradedevmodel.FieldCompanyID).
//		Scan(ctx, &v)
func (udmq *UpgradeDevModelQuery) Select(fields ...string) *UpgradeDevModelSelect {
	udmq.ctx.Fields = append(udmq.ctx.Fields, fields...)
	sbuild := &UpgradeDevModelSelect{UpgradeDevModelQuery: udmq}
	sbuild.label = upgradedevmodel.Label
	sbuild.flds, sbuild.scan = &udmq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UpgradeDevModelSelect configured with the given aggregations.
func (udmq *UpgradeDevModelQuery) Aggregate(fns ...AggregateFunc) *UpgradeDevModelSelect {
	return udmq.Select().Aggregate(fns...)
}

func (udmq *UpgradeDevModelQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range udmq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, udmq); err != nil {
				return err
			}
		}
	}
	for _, f := range udmq.ctx.Fields {
		if !upgradedevmodel.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if udmq.path != nil {
		prev, err := udmq.path(ctx)
		if err != nil {
			return err
		}
		udmq.sql = prev
	}
	return nil
}

func (udmq *UpgradeDevModelQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UpgradeDevModel, error) {
	var (
		nodes = []*UpgradeDevModel{}
		_spec = udmq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UpgradeDevModel).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UpgradeDevModel{config: udmq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, udmq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (udmq *UpgradeDevModelQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := udmq.querySpec()
	_spec.Node.Columns = udmq.ctx.Fields
	if len(udmq.ctx.Fields) > 0 {
		_spec.Unique = udmq.ctx.Unique != nil && *udmq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, udmq.driver, _spec)
}

func (udmq *UpgradeDevModelQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(upgradedevmodel.Table, upgradedevmodel.Columns, sqlgraph.NewFieldSpec(upgradedevmodel.FieldID, field.TypeInt))
	_spec.From = udmq.sql
	if unique := udmq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if udmq.path != nil {
		_spec.Unique = true
	}
	if fields := udmq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, upgradedevmodel.FieldID)
		for i := range fields {
			if fields[i] != upgradedevmodel.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := udmq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := udmq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := udmq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := udmq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (udmq *UpgradeDevModelQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(udmq.driver.Dialect())
	t1 := builder.Table(upgradedevmodel.Table)
	columns := udmq.ctx.Fields
	if len(columns) == 0 {
		columns = upgradedevmodel.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if udmq.sql != nil {
		selector = udmq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if udmq.ctx.Unique != nil && *udmq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range udmq.predicates {
		p(selector)
	}
	for _, p := range udmq.order {
		p(selector)
	}
	if offset := udmq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := udmq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UpgradeDevModelGroupBy is the group-by builder for UpgradeDevModel entities.
type UpgradeDevModelGroupBy struct {
	selector
	build *UpgradeDevModelQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (udmgb *UpgradeDevModelGroupBy) Aggregate(fns ...AggregateFunc) *UpgradeDevModelGroupBy {
	udmgb.fns = append(udmgb.fns, fns...)
	return udmgb
}

// Scan applies the selector query and scans the result into the given value.
func (udmgb *UpgradeDevModelGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, udmgb.build.ctx, ent.OpQueryGroupBy)
	if err := udmgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UpgradeDevModelQuery, *UpgradeDevModelGroupBy](ctx, udmgb.build, udmgb, udmgb.build.inters, v)
}

func (udmgb *UpgradeDevModelGroupBy) sqlScan(ctx context.Context, root *UpgradeDevModelQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(udmgb.fns))
	for _, fn := range udmgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*udmgb.flds)+len(udmgb.fns))
		for _, f := range *udmgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*udmgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := udmgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UpgradeDevModelSelect is the builder for selecting fields of UpgradeDevModel entities.
type UpgradeDevModelSelect struct {
	*UpgradeDevModelQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (udms *UpgradeDevModelSelect) Aggregate(fns ...AggregateFunc) *UpgradeDevModelSelect {
	udms.fns = append(udms.fns, fns...)
	return udms
}

// Scan applies the selector query and scans the result into the given value.
func (udms *UpgradeDevModelSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, udms.ctx, ent.OpQuerySelect)
	if err := udms.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UpgradeDevModelQuery, *UpgradeDevModelSelect](ctx, udms.UpgradeDevModelQuery, udms, udms.inters, v)
}

func (udms *UpgradeDevModelSelect) sqlScan(ctx context.Context, root *UpgradeDevModelQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(udms.fns))
	for _, fn := range udms.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*udms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := udms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
