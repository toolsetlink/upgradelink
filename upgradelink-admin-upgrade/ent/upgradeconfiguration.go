// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"upgradelink-admin-upgrade/ent/upgradeconfiguration"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// UpgradeConfiguration is the model entity for the UpgradeConfiguration schema.
type UpgradeConfiguration struct {
	config `json:"-"`
	// ID of the ent.
	// ID
	ID int `json:"id,omitempty"`
	// 公司ID
	CompanyID int `json:"company_id,omitempty"`
	// 配置唯一标识
	Key string `json:"key,omitempty"`
	// 配置名称
	Name string `json:"name,omitempty"`
	// 描述信息
	Description string `json:"description,omitempty"`
	// 是否删除 0：正常；1：已删除
	IsDel int32 `json:"is_del,omitempty"`
	// 创建时间
	CreateAt time.Time `json:"create_at,omitempty"`
	// 修改时间
	UpdateAt     time.Time `json:"update_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UpgradeConfiguration) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case upgradeconfiguration.FieldID, upgradeconfiguration.FieldCompanyID, upgradeconfiguration.FieldIsDel:
			values[i] = new(sql.NullInt64)
		case upgradeconfiguration.FieldKey, upgradeconfiguration.FieldName, upgradeconfiguration.FieldDescription:
			values[i] = new(sql.NullString)
		case upgradeconfiguration.FieldCreateAt, upgradeconfiguration.FieldUpdateAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UpgradeConfiguration fields.
func (uc *UpgradeConfiguration) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case upgradeconfiguration.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			uc.ID = int(value.Int64)
		case upgradeconfiguration.FieldCompanyID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field company_id", values[i])
			} else if value.Valid {
				uc.CompanyID = int(value.Int64)
			}
		case upgradeconfiguration.FieldKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field key", values[i])
			} else if value.Valid {
				uc.Key = value.String
			}
		case upgradeconfiguration.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				uc.Name = value.String
			}
		case upgradeconfiguration.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				uc.Description = value.String
			}
		case upgradeconfiguration.FieldIsDel:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field is_del", values[i])
			} else if value.Valid {
				uc.IsDel = int32(value.Int64)
			}
		case upgradeconfiguration.FieldCreateAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_at", values[i])
			} else if value.Valid {
				uc.CreateAt = value.Time
			}
		case upgradeconfiguration.FieldUpdateAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_at", values[i])
			} else if value.Valid {
				uc.UpdateAt = value.Time
			}
		default:
			uc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UpgradeConfiguration.
// This includes values selected through modifiers, order, etc.
func (uc *UpgradeConfiguration) Value(name string) (ent.Value, error) {
	return uc.selectValues.Get(name)
}

// Update returns a builder for updating this UpgradeConfiguration.
// Note that you need to call UpgradeConfiguration.Unwrap() before calling this method if this UpgradeConfiguration
// was returned from a transaction, and the transaction was committed or rolled back.
func (uc *UpgradeConfiguration) Update() *UpgradeConfigurationUpdateOne {
	return NewUpgradeConfigurationClient(uc.config).UpdateOne(uc)
}

// Unwrap unwraps the UpgradeConfiguration entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (uc *UpgradeConfiguration) Unwrap() *UpgradeConfiguration {
	_tx, ok := uc.config.driver.(*txDriver)
	if !ok {
		panic("ent: UpgradeConfiguration is not a transactional entity")
	}
	uc.config.driver = _tx.drv
	return uc
}

// String implements the fmt.Stringer.
func (uc *UpgradeConfiguration) String() string {
	var builder strings.Builder
	builder.WriteString("UpgradeConfiguration(")
	builder.WriteString(fmt.Sprintf("id=%v, ", uc.ID))
	builder.WriteString("company_id=")
	builder.WriteString(fmt.Sprintf("%v", uc.CompanyID))
	builder.WriteString(", ")
	builder.WriteString("key=")
	builder.WriteString(uc.Key)
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(uc.Name)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(uc.Description)
	builder.WriteString(", ")
	builder.WriteString("is_del=")
	builder.WriteString(fmt.Sprintf("%v", uc.IsDel))
	builder.WriteString(", ")
	builder.WriteString("create_at=")
	builder.WriteString(uc.CreateAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_at=")
	builder.WriteString(uc.UpdateAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// UpgradeConfigurations is a parsable slice of UpgradeConfiguration.
type UpgradeConfigurations []*UpgradeConfiguration
