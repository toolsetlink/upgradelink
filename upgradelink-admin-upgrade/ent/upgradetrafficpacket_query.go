// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"upgradelink-admin-upgrade/ent/predicate"
	"upgradelink-admin-upgrade/ent/upgradetrafficpacket"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UpgradeTrafficPacketQuery is the builder for querying UpgradeTrafficPacket entities.
type UpgradeTrafficPacketQuery struct {
	config
	ctx        *QueryContext
	order      []upgradetrafficpacket.OrderOption
	inters     []Interceptor
	predicates []predicate.UpgradeTrafficPacket
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UpgradeTrafficPacketQuery builder.
func (utpq *UpgradeTrafficPacketQuery) Where(ps ...predicate.UpgradeTrafficPacket) *UpgradeTrafficPacketQuery {
	utpq.predicates = append(utpq.predicates, ps...)
	return utpq
}

// Limit the number of records to be returned by this query.
func (utpq *UpgradeTrafficPacketQuery) Limit(limit int) *UpgradeTrafficPacketQuery {
	utpq.ctx.Limit = &limit
	return utpq
}

// Offset to start from.
func (utpq *UpgradeTrafficPacketQuery) Offset(offset int) *UpgradeTrafficPacketQuery {
	utpq.ctx.Offset = &offset
	return utpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (utpq *UpgradeTrafficPacketQuery) Unique(unique bool) *UpgradeTrafficPacketQuery {
	utpq.ctx.Unique = &unique
	return utpq
}

// Order specifies how the records should be ordered.
func (utpq *UpgradeTrafficPacketQuery) Order(o ...upgradetrafficpacket.OrderOption) *UpgradeTrafficPacketQuery {
	utpq.order = append(utpq.order, o...)
	return utpq
}

// First returns the first UpgradeTrafficPacket entity from the query.
// Returns a *NotFoundError when no UpgradeTrafficPacket was found.
func (utpq *UpgradeTrafficPacketQuery) First(ctx context.Context) (*UpgradeTrafficPacket, error) {
	nodes, err := utpq.Limit(1).All(setContextOp(ctx, utpq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{upgradetrafficpacket.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (utpq *UpgradeTrafficPacketQuery) FirstX(ctx context.Context) *UpgradeTrafficPacket {
	node, err := utpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UpgradeTrafficPacket ID from the query.
// Returns a *NotFoundError when no UpgradeTrafficPacket ID was found.
func (utpq *UpgradeTrafficPacketQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = utpq.Limit(1).IDs(setContextOp(ctx, utpq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{upgradetrafficpacket.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (utpq *UpgradeTrafficPacketQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := utpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UpgradeTrafficPacket entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UpgradeTrafficPacket entity is found.
// Returns a *NotFoundError when no UpgradeTrafficPacket entities are found.
func (utpq *UpgradeTrafficPacketQuery) Only(ctx context.Context) (*UpgradeTrafficPacket, error) {
	nodes, err := utpq.Limit(2).All(setContextOp(ctx, utpq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{upgradetrafficpacket.Label}
	default:
		return nil, &NotSingularError{upgradetrafficpacket.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (utpq *UpgradeTrafficPacketQuery) OnlyX(ctx context.Context) *UpgradeTrafficPacket {
	node, err := utpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UpgradeTrafficPacket ID in the query.
// Returns a *NotSingularError when more than one UpgradeTrafficPacket ID is found.
// Returns a *NotFoundError when no entities are found.
func (utpq *UpgradeTrafficPacketQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = utpq.Limit(2).IDs(setContextOp(ctx, utpq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{upgradetrafficpacket.Label}
	default:
		err = &NotSingularError{upgradetrafficpacket.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (utpq *UpgradeTrafficPacketQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := utpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UpgradeTrafficPackets.
func (utpq *UpgradeTrafficPacketQuery) All(ctx context.Context) ([]*UpgradeTrafficPacket, error) {
	ctx = setContextOp(ctx, utpq.ctx, ent.OpQueryAll)
	if err := utpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UpgradeTrafficPacket, *UpgradeTrafficPacketQuery]()
	return withInterceptors[[]*UpgradeTrafficPacket](ctx, utpq, qr, utpq.inters)
}

// AllX is like All, but panics if an error occurs.
func (utpq *UpgradeTrafficPacketQuery) AllX(ctx context.Context) []*UpgradeTrafficPacket {
	nodes, err := utpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UpgradeTrafficPacket IDs.
func (utpq *UpgradeTrafficPacketQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if utpq.ctx.Unique == nil && utpq.path != nil {
		utpq.Unique(true)
	}
	ctx = setContextOp(ctx, utpq.ctx, ent.OpQueryIDs)
	if err = utpq.Select(upgradetrafficpacket.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (utpq *UpgradeTrafficPacketQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := utpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (utpq *UpgradeTrafficPacketQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, utpq.ctx, ent.OpQueryCount)
	if err := utpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, utpq, querierCount[*UpgradeTrafficPacketQuery](), utpq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (utpq *UpgradeTrafficPacketQuery) CountX(ctx context.Context) int {
	count, err := utpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (utpq *UpgradeTrafficPacketQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, utpq.ctx, ent.OpQueryExist)
	switch _, err := utpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (utpq *UpgradeTrafficPacketQuery) ExistX(ctx context.Context) bool {
	exist, err := utpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UpgradeTrafficPacketQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (utpq *UpgradeTrafficPacketQuery) Clone() *UpgradeTrafficPacketQuery {
	if utpq == nil {
		return nil
	}
	return &UpgradeTrafficPacketQuery{
		config:     utpq.config,
		ctx:        utpq.ctx.Clone(),
		order:      append([]upgradetrafficpacket.OrderOption{}, utpq.order...),
		inters:     append([]Interceptor{}, utpq.inters...),
		predicates: append([]predicate.UpgradeTrafficPacket{}, utpq.predicates...),
		// clone intermediate query.
		sql:  utpq.sql.Clone(),
		path: utpq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UpgradeTrafficPacket.Query().
//		GroupBy(upgradetrafficpacket.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (utpq *UpgradeTrafficPacketQuery) GroupBy(field string, fields ...string) *UpgradeTrafficPacketGroupBy {
	utpq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UpgradeTrafficPacketGroupBy{build: utpq}
	grbuild.flds = &utpq.ctx.Fields
	grbuild.label = upgradetrafficpacket.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.UpgradeTrafficPacket.Query().
//		Select(upgradetrafficpacket.FieldName).
//		Scan(ctx, &v)
func (utpq *UpgradeTrafficPacketQuery) Select(fields ...string) *UpgradeTrafficPacketSelect {
	utpq.ctx.Fields = append(utpq.ctx.Fields, fields...)
	sbuild := &UpgradeTrafficPacketSelect{UpgradeTrafficPacketQuery: utpq}
	sbuild.label = upgradetrafficpacket.Label
	sbuild.flds, sbuild.scan = &utpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UpgradeTrafficPacketSelect configured with the given aggregations.
func (utpq *UpgradeTrafficPacketQuery) Aggregate(fns ...AggregateFunc) *UpgradeTrafficPacketSelect {
	return utpq.Select().Aggregate(fns...)
}

func (utpq *UpgradeTrafficPacketQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range utpq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, utpq); err != nil {
				return err
			}
		}
	}
	for _, f := range utpq.ctx.Fields {
		if !upgradetrafficpacket.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if utpq.path != nil {
		prev, err := utpq.path(ctx)
		if err != nil {
			return err
		}
		utpq.sql = prev
	}
	return nil
}

func (utpq *UpgradeTrafficPacketQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UpgradeTrafficPacket, error) {
	var (
		nodes = []*UpgradeTrafficPacket{}
		_spec = utpq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UpgradeTrafficPacket).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UpgradeTrafficPacket{config: utpq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, utpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (utpq *UpgradeTrafficPacketQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := utpq.querySpec()
	_spec.Node.Columns = utpq.ctx.Fields
	if len(utpq.ctx.Fields) > 0 {
		_spec.Unique = utpq.ctx.Unique != nil && *utpq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, utpq.driver, _spec)
}

func (utpq *UpgradeTrafficPacketQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(upgradetrafficpacket.Table, upgradetrafficpacket.Columns, sqlgraph.NewFieldSpec(upgradetrafficpacket.FieldID, field.TypeUint64))
	_spec.From = utpq.sql
	if unique := utpq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if utpq.path != nil {
		_spec.Unique = true
	}
	if fields := utpq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, upgradetrafficpacket.FieldID)
		for i := range fields {
			if fields[i] != upgradetrafficpacket.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := utpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := utpq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := utpq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := utpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (utpq *UpgradeTrafficPacketQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(utpq.driver.Dialect())
	t1 := builder.Table(upgradetrafficpacket.Table)
	columns := utpq.ctx.Fields
	if len(columns) == 0 {
		columns = upgradetrafficpacket.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if utpq.sql != nil {
		selector = utpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if utpq.ctx.Unique != nil && *utpq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range utpq.predicates {
		p(selector)
	}
	for _, p := range utpq.order {
		p(selector)
	}
	if offset := utpq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := utpq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UpgradeTrafficPacketGroupBy is the group-by builder for UpgradeTrafficPacket entities.
type UpgradeTrafficPacketGroupBy struct {
	selector
	build *UpgradeTrafficPacketQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (utpgb *UpgradeTrafficPacketGroupBy) Aggregate(fns ...AggregateFunc) *UpgradeTrafficPacketGroupBy {
	utpgb.fns = append(utpgb.fns, fns...)
	return utpgb
}

// Scan applies the selector query and scans the result into the given value.
func (utpgb *UpgradeTrafficPacketGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, utpgb.build.ctx, ent.OpQueryGroupBy)
	if err := utpgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UpgradeTrafficPacketQuery, *UpgradeTrafficPacketGroupBy](ctx, utpgb.build, utpgb, utpgb.build.inters, v)
}

func (utpgb *UpgradeTrafficPacketGroupBy) sqlScan(ctx context.Context, root *UpgradeTrafficPacketQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(utpgb.fns))
	for _, fn := range utpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*utpgb.flds)+len(utpgb.fns))
		for _, f := range *utpgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*utpgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := utpgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UpgradeTrafficPacketSelect is the builder for selecting fields of UpgradeTrafficPacket entities.
type UpgradeTrafficPacketSelect struct {
	*UpgradeTrafficPacketQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (utps *UpgradeTrafficPacketSelect) Aggregate(fns ...AggregateFunc) *UpgradeTrafficPacketSelect {
	utps.fns = append(utps.fns, fns...)
	return utps
}

// Scan applies the selector query and scans the result into the given value.
func (utps *UpgradeTrafficPacketSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, utps.ctx, ent.OpQuerySelect)
	if err := utps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UpgradeTrafficPacketQuery, *UpgradeTrafficPacketSelect](ctx, utps.UpgradeTrafficPacketQuery, utps, utps.inters, v)
}

func (utps *UpgradeTrafficPacketSelect) sqlScan(ctx context.Context, root *UpgradeTrafficPacketQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(utps.fns))
	for _, fn := range utps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*utps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := utps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
