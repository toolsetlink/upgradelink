// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"upgradelink-admin-upgrade/ent/upgradefile"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// UpgradeFile is the model entity for the UpgradeFile schema.
type UpgradeFile struct {
	config `json:"-"`
	// ID of the ent.
	// ID
	ID int `json:"id,omitempty"`
	// 公司ID
	CompanyID int `json:"company_id,omitempty"`
	// 文件应用唯一标识
	Key string `json:"key,omitempty"`
	// 文件应用名称
	Name string `json:"name,omitempty"`
	// 描述信息
	Description string `json:"description,omitempty"`
	// 是否删除 0：正常；1：已删除
	IsDel int32 `json:"is_del,omitempty"`
	// 创建时间
	CreateAt time.Time `json:"create_at,omitempty"`
	// 修改时间
	UpdateAt     time.Time `json:"update_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UpgradeFile) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case upgradefile.FieldID, upgradefile.FieldCompanyID, upgradefile.FieldIsDel:
			values[i] = new(sql.NullInt64)
		case upgradefile.FieldKey, upgradefile.FieldName, upgradefile.FieldDescription:
			values[i] = new(sql.NullString)
		case upgradefile.FieldCreateAt, upgradefile.FieldUpdateAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UpgradeFile fields.
func (uf *UpgradeFile) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case upgradefile.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			uf.ID = int(value.Int64)
		case upgradefile.FieldCompanyID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field company_id", values[i])
			} else if value.Valid {
				uf.CompanyID = int(value.Int64)
			}
		case upgradefile.FieldKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field key", values[i])
			} else if value.Valid {
				uf.Key = value.String
			}
		case upgradefile.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				uf.Name = value.String
			}
		case upgradefile.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				uf.Description = value.String
			}
		case upgradefile.FieldIsDel:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field is_del", values[i])
			} else if value.Valid {
				uf.IsDel = int32(value.Int64)
			}
		case upgradefile.FieldCreateAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_at", values[i])
			} else if value.Valid {
				uf.CreateAt = value.Time
			}
		case upgradefile.FieldUpdateAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_at", values[i])
			} else if value.Valid {
				uf.UpdateAt = value.Time
			}
		default:
			uf.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UpgradeFile.
// This includes values selected through modifiers, order, etc.
func (uf *UpgradeFile) Value(name string) (ent.Value, error) {
	return uf.selectValues.Get(name)
}

// Update returns a builder for updating this UpgradeFile.
// Note that you need to call UpgradeFile.Unwrap() before calling this method if this UpgradeFile
// was returned from a transaction, and the transaction was committed or rolled back.
func (uf *UpgradeFile) Update() *UpgradeFileUpdateOne {
	return NewUpgradeFileClient(uf.config).UpdateOne(uf)
}

// Unwrap unwraps the UpgradeFile entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (uf *UpgradeFile) Unwrap() *UpgradeFile {
	_tx, ok := uf.config.driver.(*txDriver)
	if !ok {
		panic("ent: UpgradeFile is not a transactional entity")
	}
	uf.config.driver = _tx.drv
	return uf
}

// String implements the fmt.Stringer.
func (uf *UpgradeFile) String() string {
	var builder strings.Builder
	builder.WriteString("UpgradeFile(")
	builder.WriteString(fmt.Sprintf("id=%v, ", uf.ID))
	builder.WriteString("company_id=")
	builder.WriteString(fmt.Sprintf("%v", uf.CompanyID))
	builder.WriteString(", ")
	builder.WriteString("key=")
	builder.WriteString(uf.Key)
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(uf.Name)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(uf.Description)
	builder.WriteString(", ")
	builder.WriteString("is_del=")
	builder.WriteString(fmt.Sprintf("%v", uf.IsDel))
	builder.WriteString(", ")
	builder.WriteString("create_at=")
	builder.WriteString(uf.CreateAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_at=")
	builder.WriteString(uf.UpdateAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// UpgradeFiles is a parsable slice of UpgradeFile.
type UpgradeFiles []*UpgradeFile
