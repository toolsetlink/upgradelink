// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"upgradelink-admin-upgrade/ent/predicate"
	"upgradelink-admin-upgrade/ent/upgradeelectronupgradestrategy"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UpgradeElectronUpgradeStrategyQuery is the builder for querying UpgradeElectronUpgradeStrategy entities.
type UpgradeElectronUpgradeStrategyQuery struct {
	config
	ctx        *QueryContext
	order      []upgradeelectronupgradestrategy.OrderOption
	inters     []Interceptor
	predicates []predicate.UpgradeElectronUpgradeStrategy
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UpgradeElectronUpgradeStrategyQuery builder.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) Where(ps ...predicate.UpgradeElectronUpgradeStrategy) *UpgradeElectronUpgradeStrategyQuery {
	ueusq.predicates = append(ueusq.predicates, ps...)
	return ueusq
}

// Limit the number of records to be returned by this query.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) Limit(limit int) *UpgradeElectronUpgradeStrategyQuery {
	ueusq.ctx.Limit = &limit
	return ueusq
}

// Offset to start from.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) Offset(offset int) *UpgradeElectronUpgradeStrategyQuery {
	ueusq.ctx.Offset = &offset
	return ueusq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) Unique(unique bool) *UpgradeElectronUpgradeStrategyQuery {
	ueusq.ctx.Unique = &unique
	return ueusq
}

// Order specifies how the records should be ordered.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) Order(o ...upgradeelectronupgradestrategy.OrderOption) *UpgradeElectronUpgradeStrategyQuery {
	ueusq.order = append(ueusq.order, o...)
	return ueusq
}

// First returns the first UpgradeElectronUpgradeStrategy entity from the query.
// Returns a *NotFoundError when no UpgradeElectronUpgradeStrategy was found.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) First(ctx context.Context) (*UpgradeElectronUpgradeStrategy, error) {
	nodes, err := ueusq.Limit(1).All(setContextOp(ctx, ueusq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{upgradeelectronupgradestrategy.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) FirstX(ctx context.Context) *UpgradeElectronUpgradeStrategy {
	node, err := ueusq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UpgradeElectronUpgradeStrategy ID from the query.
// Returns a *NotFoundError when no UpgradeElectronUpgradeStrategy ID was found.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ueusq.Limit(1).IDs(setContextOp(ctx, ueusq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{upgradeelectronupgradestrategy.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) FirstIDX(ctx context.Context) int {
	id, err := ueusq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UpgradeElectronUpgradeStrategy entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UpgradeElectronUpgradeStrategy entity is found.
// Returns a *NotFoundError when no UpgradeElectronUpgradeStrategy entities are found.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) Only(ctx context.Context) (*UpgradeElectronUpgradeStrategy, error) {
	nodes, err := ueusq.Limit(2).All(setContextOp(ctx, ueusq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{upgradeelectronupgradestrategy.Label}
	default:
		return nil, &NotSingularError{upgradeelectronupgradestrategy.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) OnlyX(ctx context.Context) *UpgradeElectronUpgradeStrategy {
	node, err := ueusq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UpgradeElectronUpgradeStrategy ID in the query.
// Returns a *NotSingularError when more than one UpgradeElectronUpgradeStrategy ID is found.
// Returns a *NotFoundError when no entities are found.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ueusq.Limit(2).IDs(setContextOp(ctx, ueusq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{upgradeelectronupgradestrategy.Label}
	default:
		err = &NotSingularError{upgradeelectronupgradestrategy.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) OnlyIDX(ctx context.Context) int {
	id, err := ueusq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UpgradeElectronUpgradeStrategies.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) All(ctx context.Context) ([]*UpgradeElectronUpgradeStrategy, error) {
	ctx = setContextOp(ctx, ueusq.ctx, ent.OpQueryAll)
	if err := ueusq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UpgradeElectronUpgradeStrategy, *UpgradeElectronUpgradeStrategyQuery]()
	return withInterceptors[[]*UpgradeElectronUpgradeStrategy](ctx, ueusq, qr, ueusq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) AllX(ctx context.Context) []*UpgradeElectronUpgradeStrategy {
	nodes, err := ueusq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UpgradeElectronUpgradeStrategy IDs.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ueusq.ctx.Unique == nil && ueusq.path != nil {
		ueusq.Unique(true)
	}
	ctx = setContextOp(ctx, ueusq.ctx, ent.OpQueryIDs)
	if err = ueusq.Select(upgradeelectronupgradestrategy.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) IDsX(ctx context.Context) []int {
	ids, err := ueusq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ueusq.ctx, ent.OpQueryCount)
	if err := ueusq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ueusq, querierCount[*UpgradeElectronUpgradeStrategyQuery](), ueusq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) CountX(ctx context.Context) int {
	count, err := ueusq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ueusq.ctx, ent.OpQueryExist)
	switch _, err := ueusq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) ExistX(ctx context.Context) bool {
	exist, err := ueusq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UpgradeElectronUpgradeStrategyQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) Clone() *UpgradeElectronUpgradeStrategyQuery {
	if ueusq == nil {
		return nil
	}
	return &UpgradeElectronUpgradeStrategyQuery{
		config:     ueusq.config,
		ctx:        ueusq.ctx.Clone(),
		order:      append([]upgradeelectronupgradestrategy.OrderOption{}, ueusq.order...),
		inters:     append([]Interceptor{}, ueusq.inters...),
		predicates: append([]predicate.UpgradeElectronUpgradeStrategy{}, ueusq.predicates...),
		// clone intermediate query.
		sql:  ueusq.sql.Clone(),
		path: ueusq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CompanyID int `json:"company_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UpgradeElectronUpgradeStrategy.Query().
//		GroupBy(upgradeelectronupgradestrategy.FieldCompanyID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ueusq *UpgradeElectronUpgradeStrategyQuery) GroupBy(field string, fields ...string) *UpgradeElectronUpgradeStrategyGroupBy {
	ueusq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UpgradeElectronUpgradeStrategyGroupBy{build: ueusq}
	grbuild.flds = &ueusq.ctx.Fields
	grbuild.label = upgradeelectronupgradestrategy.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CompanyID int `json:"company_id,omitempty"`
//	}
//
//	client.UpgradeElectronUpgradeStrategy.Query().
//		Select(upgradeelectronupgradestrategy.FieldCompanyID).
//		Scan(ctx, &v)
func (ueusq *UpgradeElectronUpgradeStrategyQuery) Select(fields ...string) *UpgradeElectronUpgradeStrategySelect {
	ueusq.ctx.Fields = append(ueusq.ctx.Fields, fields...)
	sbuild := &UpgradeElectronUpgradeStrategySelect{UpgradeElectronUpgradeStrategyQuery: ueusq}
	sbuild.label = upgradeelectronupgradestrategy.Label
	sbuild.flds, sbuild.scan = &ueusq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UpgradeElectronUpgradeStrategySelect configured with the given aggregations.
func (ueusq *UpgradeElectronUpgradeStrategyQuery) Aggregate(fns ...AggregateFunc) *UpgradeElectronUpgradeStrategySelect {
	return ueusq.Select().Aggregate(fns...)
}

func (ueusq *UpgradeElectronUpgradeStrategyQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ueusq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ueusq); err != nil {
				return err
			}
		}
	}
	for _, f := range ueusq.ctx.Fields {
		if !upgradeelectronupgradestrategy.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ueusq.path != nil {
		prev, err := ueusq.path(ctx)
		if err != nil {
			return err
		}
		ueusq.sql = prev
	}
	return nil
}

func (ueusq *UpgradeElectronUpgradeStrategyQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UpgradeElectronUpgradeStrategy, error) {
	var (
		nodes = []*UpgradeElectronUpgradeStrategy{}
		_spec = ueusq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UpgradeElectronUpgradeStrategy).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UpgradeElectronUpgradeStrategy{config: ueusq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ueusq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ueusq *UpgradeElectronUpgradeStrategyQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ueusq.querySpec()
	_spec.Node.Columns = ueusq.ctx.Fields
	if len(ueusq.ctx.Fields) > 0 {
		_spec.Unique = ueusq.ctx.Unique != nil && *ueusq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ueusq.driver, _spec)
}

func (ueusq *UpgradeElectronUpgradeStrategyQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(upgradeelectronupgradestrategy.Table, upgradeelectronupgradestrategy.Columns, sqlgraph.NewFieldSpec(upgradeelectronupgradestrategy.FieldID, field.TypeInt))
	_spec.From = ueusq.sql
	if unique := ueusq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ueusq.path != nil {
		_spec.Unique = true
	}
	if fields := ueusq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, upgradeelectronupgradestrategy.FieldID)
		for i := range fields {
			if fields[i] != upgradeelectronupgradestrategy.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ueusq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ueusq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ueusq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ueusq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ueusq *UpgradeElectronUpgradeStrategyQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ueusq.driver.Dialect())
	t1 := builder.Table(upgradeelectronupgradestrategy.Table)
	columns := ueusq.ctx.Fields
	if len(columns) == 0 {
		columns = upgradeelectronupgradestrategy.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ueusq.sql != nil {
		selector = ueusq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ueusq.ctx.Unique != nil && *ueusq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ueusq.predicates {
		p(selector)
	}
	for _, p := range ueusq.order {
		p(selector)
	}
	if offset := ueusq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ueusq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UpgradeElectronUpgradeStrategyGroupBy is the group-by builder for UpgradeElectronUpgradeStrategy entities.
type UpgradeElectronUpgradeStrategyGroupBy struct {
	selector
	build *UpgradeElectronUpgradeStrategyQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ueusgb *UpgradeElectronUpgradeStrategyGroupBy) Aggregate(fns ...AggregateFunc) *UpgradeElectronUpgradeStrategyGroupBy {
	ueusgb.fns = append(ueusgb.fns, fns...)
	return ueusgb
}

// Scan applies the selector query and scans the result into the given value.
func (ueusgb *UpgradeElectronUpgradeStrategyGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ueusgb.build.ctx, ent.OpQueryGroupBy)
	if err := ueusgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UpgradeElectronUpgradeStrategyQuery, *UpgradeElectronUpgradeStrategyGroupBy](ctx, ueusgb.build, ueusgb, ueusgb.build.inters, v)
}

func (ueusgb *UpgradeElectronUpgradeStrategyGroupBy) sqlScan(ctx context.Context, root *UpgradeElectronUpgradeStrategyQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ueusgb.fns))
	for _, fn := range ueusgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ueusgb.flds)+len(ueusgb.fns))
		for _, f := range *ueusgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ueusgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ueusgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UpgradeElectronUpgradeStrategySelect is the builder for selecting fields of UpgradeElectronUpgradeStrategy entities.
type UpgradeElectronUpgradeStrategySelect struct {
	*UpgradeElectronUpgradeStrategyQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ueuss *UpgradeElectronUpgradeStrategySelect) Aggregate(fns ...AggregateFunc) *UpgradeElectronUpgradeStrategySelect {
	ueuss.fns = append(ueuss.fns, fns...)
	return ueuss
}

// Scan applies the selector query and scans the result into the given value.
func (ueuss *UpgradeElectronUpgradeStrategySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ueuss.ctx, ent.OpQuerySelect)
	if err := ueuss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UpgradeElectronUpgradeStrategyQuery, *UpgradeElectronUpgradeStrategySelect](ctx, ueuss.UpgradeElectronUpgradeStrategyQuery, ueuss, ueuss.inters, v)
}

func (ueuss *UpgradeElectronUpgradeStrategySelect) sqlScan(ctx context.Context, root *UpgradeElectronUpgradeStrategyQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ueuss.fns))
	for _, fn := range ueuss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ueuss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ueuss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
