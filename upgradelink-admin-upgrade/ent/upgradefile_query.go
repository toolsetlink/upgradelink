// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"upgradelink-admin-upgrade/ent/predicate"
	"upgradelink-admin-upgrade/ent/upgradefile"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UpgradeFileQuery is the builder for querying UpgradeFile entities.
type UpgradeFileQuery struct {
	config
	ctx        *QueryContext
	order      []upgradefile.OrderOption
	inters     []Interceptor
	predicates []predicate.UpgradeFile
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UpgradeFileQuery builder.
func (ufq *UpgradeFileQuery) Where(ps ...predicate.UpgradeFile) *UpgradeFileQuery {
	ufq.predicates = append(ufq.predicates, ps...)
	return ufq
}

// Limit the number of records to be returned by this query.
func (ufq *UpgradeFileQuery) Limit(limit int) *UpgradeFileQuery {
	ufq.ctx.Limit = &limit
	return ufq
}

// Offset to start from.
func (ufq *UpgradeFileQuery) Offset(offset int) *UpgradeFileQuery {
	ufq.ctx.Offset = &offset
	return ufq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ufq *UpgradeFileQuery) Unique(unique bool) *UpgradeFileQuery {
	ufq.ctx.Unique = &unique
	return ufq
}

// Order specifies how the records should be ordered.
func (ufq *UpgradeFileQuery) Order(o ...upgradefile.OrderOption) *UpgradeFileQuery {
	ufq.order = append(ufq.order, o...)
	return ufq
}

// First returns the first UpgradeFile entity from the query.
// Returns a *NotFoundError when no UpgradeFile was found.
func (ufq *UpgradeFileQuery) First(ctx context.Context) (*UpgradeFile, error) {
	nodes, err := ufq.Limit(1).All(setContextOp(ctx, ufq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{upgradefile.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ufq *UpgradeFileQuery) FirstX(ctx context.Context) *UpgradeFile {
	node, err := ufq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UpgradeFile ID from the query.
// Returns a *NotFoundError when no UpgradeFile ID was found.
func (ufq *UpgradeFileQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ufq.Limit(1).IDs(setContextOp(ctx, ufq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{upgradefile.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ufq *UpgradeFileQuery) FirstIDX(ctx context.Context) int {
	id, err := ufq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UpgradeFile entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UpgradeFile entity is found.
// Returns a *NotFoundError when no UpgradeFile entities are found.
func (ufq *UpgradeFileQuery) Only(ctx context.Context) (*UpgradeFile, error) {
	nodes, err := ufq.Limit(2).All(setContextOp(ctx, ufq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{upgradefile.Label}
	default:
		return nil, &NotSingularError{upgradefile.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ufq *UpgradeFileQuery) OnlyX(ctx context.Context) *UpgradeFile {
	node, err := ufq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UpgradeFile ID in the query.
// Returns a *NotSingularError when more than one UpgradeFile ID is found.
// Returns a *NotFoundError when no entities are found.
func (ufq *UpgradeFileQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ufq.Limit(2).IDs(setContextOp(ctx, ufq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{upgradefile.Label}
	default:
		err = &NotSingularError{upgradefile.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ufq *UpgradeFileQuery) OnlyIDX(ctx context.Context) int {
	id, err := ufq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UpgradeFiles.
func (ufq *UpgradeFileQuery) All(ctx context.Context) ([]*UpgradeFile, error) {
	ctx = setContextOp(ctx, ufq.ctx, ent.OpQueryAll)
	if err := ufq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UpgradeFile, *UpgradeFileQuery]()
	return withInterceptors[[]*UpgradeFile](ctx, ufq, qr, ufq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ufq *UpgradeFileQuery) AllX(ctx context.Context) []*UpgradeFile {
	nodes, err := ufq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UpgradeFile IDs.
func (ufq *UpgradeFileQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ufq.ctx.Unique == nil && ufq.path != nil {
		ufq.Unique(true)
	}
	ctx = setContextOp(ctx, ufq.ctx, ent.OpQueryIDs)
	if err = ufq.Select(upgradefile.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ufq *UpgradeFileQuery) IDsX(ctx context.Context) []int {
	ids, err := ufq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ufq *UpgradeFileQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ufq.ctx, ent.OpQueryCount)
	if err := ufq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ufq, querierCount[*UpgradeFileQuery](), ufq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ufq *UpgradeFileQuery) CountX(ctx context.Context) int {
	count, err := ufq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ufq *UpgradeFileQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ufq.ctx, ent.OpQueryExist)
	switch _, err := ufq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ufq *UpgradeFileQuery) ExistX(ctx context.Context) bool {
	exist, err := ufq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UpgradeFileQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ufq *UpgradeFileQuery) Clone() *UpgradeFileQuery {
	if ufq == nil {
		return nil
	}
	return &UpgradeFileQuery{
		config:     ufq.config,
		ctx:        ufq.ctx.Clone(),
		order:      append([]upgradefile.OrderOption{}, ufq.order...),
		inters:     append([]Interceptor{}, ufq.inters...),
		predicates: append([]predicate.UpgradeFile{}, ufq.predicates...),
		// clone intermediate query.
		sql:  ufq.sql.Clone(),
		path: ufq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CompanyID int `json:"company_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UpgradeFile.Query().
//		GroupBy(upgradefile.FieldCompanyID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ufq *UpgradeFileQuery) GroupBy(field string, fields ...string) *UpgradeFileGroupBy {
	ufq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UpgradeFileGroupBy{build: ufq}
	grbuild.flds = &ufq.ctx.Fields
	grbuild.label = upgradefile.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CompanyID int `json:"company_id,omitempty"`
//	}
//
//	client.UpgradeFile.Query().
//		Select(upgradefile.FieldCompanyID).
//		Scan(ctx, &v)
func (ufq *UpgradeFileQuery) Select(fields ...string) *UpgradeFileSelect {
	ufq.ctx.Fields = append(ufq.ctx.Fields, fields...)
	sbuild := &UpgradeFileSelect{UpgradeFileQuery: ufq}
	sbuild.label = upgradefile.Label
	sbuild.flds, sbuild.scan = &ufq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UpgradeFileSelect configured with the given aggregations.
func (ufq *UpgradeFileQuery) Aggregate(fns ...AggregateFunc) *UpgradeFileSelect {
	return ufq.Select().Aggregate(fns...)
}

func (ufq *UpgradeFileQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ufq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ufq); err != nil {
				return err
			}
		}
	}
	for _, f := range ufq.ctx.Fields {
		if !upgradefile.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ufq.path != nil {
		prev, err := ufq.path(ctx)
		if err != nil {
			return err
		}
		ufq.sql = prev
	}
	return nil
}

func (ufq *UpgradeFileQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UpgradeFile, error) {
	var (
		nodes = []*UpgradeFile{}
		_spec = ufq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UpgradeFile).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UpgradeFile{config: ufq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ufq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ufq *UpgradeFileQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ufq.querySpec()
	_spec.Node.Columns = ufq.ctx.Fields
	if len(ufq.ctx.Fields) > 0 {
		_spec.Unique = ufq.ctx.Unique != nil && *ufq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ufq.driver, _spec)
}

func (ufq *UpgradeFileQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(upgradefile.Table, upgradefile.Columns, sqlgraph.NewFieldSpec(upgradefile.FieldID, field.TypeInt))
	_spec.From = ufq.sql
	if unique := ufq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ufq.path != nil {
		_spec.Unique = true
	}
	if fields := ufq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, upgradefile.FieldID)
		for i := range fields {
			if fields[i] != upgradefile.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ufq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ufq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ufq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ufq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ufq *UpgradeFileQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ufq.driver.Dialect())
	t1 := builder.Table(upgradefile.Table)
	columns := ufq.ctx.Fields
	if len(columns) == 0 {
		columns = upgradefile.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ufq.sql != nil {
		selector = ufq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ufq.ctx.Unique != nil && *ufq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ufq.predicates {
		p(selector)
	}
	for _, p := range ufq.order {
		p(selector)
	}
	if offset := ufq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ufq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UpgradeFileGroupBy is the group-by builder for UpgradeFile entities.
type UpgradeFileGroupBy struct {
	selector
	build *UpgradeFileQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ufgb *UpgradeFileGroupBy) Aggregate(fns ...AggregateFunc) *UpgradeFileGroupBy {
	ufgb.fns = append(ufgb.fns, fns...)
	return ufgb
}

// Scan applies the selector query and scans the result into the given value.
func (ufgb *UpgradeFileGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ufgb.build.ctx, ent.OpQueryGroupBy)
	if err := ufgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UpgradeFileQuery, *UpgradeFileGroupBy](ctx, ufgb.build, ufgb, ufgb.build.inters, v)
}

func (ufgb *UpgradeFileGroupBy) sqlScan(ctx context.Context, root *UpgradeFileQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ufgb.fns))
	for _, fn := range ufgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ufgb.flds)+len(ufgb.fns))
		for _, f := range *ufgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ufgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ufgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UpgradeFileSelect is the builder for selecting fields of UpgradeFile entities.
type UpgradeFileSelect struct {
	*UpgradeFileQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ufs *UpgradeFileSelect) Aggregate(fns ...AggregateFunc) *UpgradeFileSelect {
	ufs.fns = append(ufs.fns, fns...)
	return ufs
}

// Scan applies the selector query and scans the result into the given value.
func (ufs *UpgradeFileSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ufs.ctx, ent.OpQuerySelect)
	if err := ufs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UpgradeFileQuery, *UpgradeFileSelect](ctx, ufs.UpgradeFileQuery, ufs, ufs.inters, v)
}

func (ufs *UpgradeFileSelect) sqlScan(ctx context.Context, root *UpgradeFileQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ufs.fns))
	for _, fn := range ufs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ufs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ufs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
